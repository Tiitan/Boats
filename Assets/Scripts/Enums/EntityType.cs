namespace Enums
{
    /// <summary>
    /// used by MainBoat and selectable.
    /// TODO: Use layer instead ?
    /// </summary>
    public enum EntityType
    {
        Resource,
        Ship,
        Structure
    }
}